import { View, Text } from 'react-native';
import { styles } from '../../styles/index.styles.js';

export default function NewsFeed() {
    return (
        <View style={styles.container}>
            <Text>Welcome to the News Feed!</Text>
        </View>
    );
}
