import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    // ===== Main content =====
    mainContent: {
        flex: 1,
        backgroundColor: '#fff',
        margin: 8,
        borderRadius: 16,
        padding: 20,
    },



});