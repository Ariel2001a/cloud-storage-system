version: "3.9"

services:
  tests:
    build:
      context: ..
      dockerfile: test/Dockerfile
    volumes:
      - ./newFiles:/usr/src/app/newFiles
    networks:
      - local

volumes:
  newFiles:

networks:
  local:
    driver: bridge
