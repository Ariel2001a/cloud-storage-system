services:
  tests:
    build:
      context: ..
      dockerfile: test/Dockerfile
    volumes:
      - testdata:/usr/src/app/newFiles
    networks:
      - local

volumes:
  testdata:

networks:
  local:
    driver: bridge
